<?php

// namespace App\Http\Controllers;
// use whatever;

// class ViewController extends Controller
// {
//     public function index()
//     {
//         connect database, acquire an array of values, then check array length and initialize counter
       
//         $databaseValues=Database::all();
//         $dbLength = count($databaseValues);
//         $dbCounter=0;
        
//         loop through values, incrementing the counter to get the sum
        
//         for ($x = 0; $x < $dbLength; $x++) {
//             $dbCounter=$databaseValues[$x]+$dbCounter;
//         } 

//         compute the average after the loop has run its course, return the view and pass in the variable to display in view

//         $average=$dbCounter/$dbLength;
//         return view('welcome',$average);

//     }
// }

